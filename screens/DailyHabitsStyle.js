import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },

  header: {
    fontSize: 50,
    marginTop: "10%",
    color: "#000",
    fontWeight: "800",
  },
});

export default styles;